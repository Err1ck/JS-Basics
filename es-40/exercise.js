/*
# Exercise 40

Create an object literal called `car` with two properties: `name` and `color`.
Print in the console values of the properties.
*/

let car={name:"erick",color:"red"}

console.log(car);